// TODO: Controller para dashboard - necessário implementar métodos no UserService
// import { Controller, Get, UseGuards } from '@nestjs/common';
// import { ApiOkResponse, ApiTags, ApiUnauthorizedResponse, ApiBearerAuth, ApiForbiddenResponse, ApiOperation } from '@nestjs/swagger';
// import { UsersService } from '../users/users.service';
// import { AdminProfessorGuard } from '../auth/guards/admin-professor.guard';

// Este controller fornece endpoints úteis para dashboards e estatísticas do sistema
// Para implementar, adicione os seguintes métodos no UsersService:
// - countAlunos(), countProfessores(), countCursos(), countDisciplinas(), countTurmas(), countMatriculas()
// - findRecentUsers(limit), findRecentTurmas(limit), findRecentMatriculas(limit)

export class DashboardController {
  // Implementação temporariamente comentada
}
